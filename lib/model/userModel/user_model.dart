class UserModel{
  String ? userName;
  String ? userEmail;
  String ? phoneNumber;
  String ? image;
  String ? address;
  UserModel({
    required this.userName,
    required this.userEmail,
    required this.phoneNumber,
    required this.image,
    required this.address
  });
}
